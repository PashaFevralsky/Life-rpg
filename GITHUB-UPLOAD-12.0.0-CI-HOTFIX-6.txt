Replace only personal-os.e2e.test.js in repository root.
Fix: after focusStop(), the E2E re-reads the normalized task from taskAll() instead of checking a stale object reference.
Runtime/state are unchanged. STATE_VERSION remains 18.
