import { test, expect } from "@playwright/test";
async function boot(page){const errors=[];page.on("pageerror",e=>errors.push(String(e)));await page.goto("/",{waitUntil:"domcontentloaded"});await expect(page.locator("html")).not.toHaveClass(/life-rpg-booting/);await page.evaluate(()=>{switchTab("more");ux7SetView("more","settings",false)});return errors}
test("Android Acceptance 13.1.1 checklist persists and diagnostics run",async({page})=>{const errors=await boot(page);await expect(page.locator("#share131Acceptance")).toContainText("Android Acceptance 13.1.1");await page.evaluate(()=>share131AcceptanceSet("text-share","pass","E2E pass"));await expect(page.locator("#share131Acceptance")).toContainText("1/5 PASS");expect(await page.evaluate(()=>share131AcceptanceSummary().rows.find(x=>x.id==="text-share")?.status)).toBe("pass");await page.getByRole("button",{name:"Проверить телефон"}).click();await expect(page.locator("#share131Acceptance")).toContainText("IndexedDB Share Queue");expect(errors).toEqual([])});
test("Android Acceptance 13.1.1 generates test ICS",async({page})=>{const errors=await boot(page);const downloadPromise=page.waitForEvent("download");await page.getByRole("button",{name:"Тест ICS"}).click();const download=await downloadPromise;expect(download.suggestedFilename()).toBe("life-rpg-android-acceptance.ics");expect(errors).toEqual([])});
